import React from 'react';

import classes from './Order.css';

const order = (props) => (
  <div className={classes.Order}>
    <p>Ingredienser:</p>
    <p>Pris: <strong></strong></p>
  </div>
);

export default order;
